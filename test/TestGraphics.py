from graphics import *
from math import cos
from math import sin, sqrt, trunc, radians

"""__win_width = 1920
__win_height = 1080
__node_size = 250
__radius = 60

__instance_x = __radius

__furthest_up = None
__furthest_down = None
__furthest_left = None
__furthest_right = None

__start_node_point = Point((__win_width/2) + __radius, __win_height/2)
__graph_nodes = []

window = GraphWin("My Graph", __win_width, __win_height)

    point_one_x = 280*sqrt(2.0)*cos(radians(45))
    point_one_y = 220*sqrt(2.0)*sin(radians(45))
    print("Point One X: " + str(point_one_x))
    print("Point One Y: " + str(point_one_y))

    #drawing functions
    circle.draw(window)
    line_one.draw(window)

    circle.setFill("Blue")
    window.getMouse()
    window.close()"""
